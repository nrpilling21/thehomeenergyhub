{
  "name": "thehomeenergyhub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "/* check:links */": "Strict inbound-internal-link check (BL-129). The blog agent runs this before publishing; it exits non-zero on a broken link or a route with fewer than 3 inbound links.",
    "check:links": "node scripts/check-internal-links.mjs",
    "build": "node scripts/check-internal-links.mjs --warn-only && next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "next": "14.2.15",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10",
    "eslint": "^8",
    "eslint-config-next": "14.2.15",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
